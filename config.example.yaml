app:
  env: "local"
  name: "ichi-go"

http:
  timeout: 10000
  port: 8080
  cors:
    allowedOrigins: ["*"]

web:
  port: 8081

database:
  driver: "mysql"
  host: "localhost"
  port: 3306
  user: "root"
  password: "password"
  name: "mydb"
  maxIdleConns: 10
  maxOpenConns: 100
  maxLifetime: 3600

log:
  level: "debug"
  requestId:
    driver: "default"